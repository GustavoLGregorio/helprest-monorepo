version: '3.8'

services:
  api:
    build:
      context: ./helprest-api
      dockerfile: Dockerfile
    container_name: helprest-api-prd
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
